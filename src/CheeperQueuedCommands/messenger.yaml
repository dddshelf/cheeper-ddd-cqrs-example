#snippet messenger-config
framework:
    messenger:
        transports:
            async:
                dsn: 'amqp://guest:guest@localhost:5672/%2f/messages'
            sync: 'sync://'
        routing:
            'CheeperQueuedCommands\QueuedCommand': async
# end-snippet
